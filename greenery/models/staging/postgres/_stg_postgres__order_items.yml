version: 2

models:
  - name: stg_postgres__order_items
    description: base table from postgres sources
    columns: 
      - name: order_id
        tests:
        - not_null
      - name: product_id
      - name: quantity